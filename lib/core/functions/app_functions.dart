double sizeCalculator(double size, double ratio) {
  return size * (ratio / 100);
}

double dynamicHeightCalculator(double size, double ratio) {
  return size * (ratio / 100);
}

double dyanmicWidthCalculator(double size, double ratio) {
  return size * (ratio / 100);
}